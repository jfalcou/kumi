##======================================================================================================================
##  KUMI - Compact C++20 Tuple Toolbox
##  Copyright : KUMI Project Contributors
##  SPDX-License-Identifier: BSL-1.0
##======================================================================================================================
name: Nvidia Tests
on:
  workflow_call:

jobs:
  linux:
    name: ${{ matrix.compiler.preset }} (${{ matrix.options }})
    runs-on: self-hosted
    strategy:
      fail-fast: false
      matrix:
        compiler:
          - { preset: "clang-cuda"                                }
          - { preset: "nvcc"                                      }
          - { preset: "nvc++", setup: "./.github/scripts/cuda.sh" }
          - { preset: "nvcc-g++"                                  }
          - { preset: "nvcc-clang++"                              }

        options: [Debug, Release]

    steps:
      - name: Fetch current branch
        uses: actions/checkout@v6

      - name: Configure CMake and Build
        uses: ./.github/actions/config
        with:
          config: ${{ matrix.options }}
          preset: ${{ matrix.compiler.preset }}
          setup-script: ${{ matrix.compiler.setup }}
          targets: ${{ matrix.options == 'Debug' && 'unit.exe doc.exe' || 'unit.exe'}}

      - name: Run Unit Tests
        uses: ./.github/actions/test
        with:
          config: ${{ matrix.options }}
          preset: ${{ matrix.compiler.preset }}
          targets: ${{ matrix.options == 'Debug' && 'unit|doc' || 'unit'}}
